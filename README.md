Server code originally forked from https://github.com/antholeole/actix-sockets

To run server locally, 
``` cd actix-server && cargo run ```

To run an instance of the client,
``` cd actix-react-client && npm run start```
